node_modules/
dist/

# Downloaded test corpus (regenerate via `npm run corpus:build`).
corpus/golden/
corpus/staging/
corpus/db_export/

# External reference clones (e.g. e621ng/dtext for Ragel grammar lookup).
ref/

# Per-machine bench snapshots (regenerate via `scripts/bench-parse.ts`).
scripts/bench-snapshots/

# Workbench scratch from scripts/evil-* harness.
.evil-failures.json
.evil-probe.json
